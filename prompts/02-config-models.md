Implement src/config.py using python-dotenv and pydantic-settings to load all environment variables from CLAUDE.md. Then implement src/models/base.py, src/models/new_hire.py, and src/models/termination.py exactly as specified in CLAUDE.md with all validators. Add tests in tests/test_models.py for both models — test valid data, invalid states, invalid pay frequency, invalid termination reasons, and edge cases. Run the tests and make sure they pass.