Implement src/main.py as the entry point that initializes logging, loads config, creates the bot, and starts polling. Wire up the /newhire and /terminate handlers so they call the real ADP functions (login → navigate → fill form → report back to Telegram). Make sure browser context is properly cleaned up after each automation run using try/finally. Add a README.md with setup instructions, usage examples for both commands, and a note about updating ADP selectors. Do a final review of all files for PEP 8 compliance, type hints, and docstrings.