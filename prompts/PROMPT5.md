Implement src/telegram_bot/parsers.py to parse the /newhire and /terminate message formats from CLAUDE.md into their respective Pydantic models. Handle malformed input gracefully with clear error messages. Then implement src/telegram_bot/handlers/new_hire.py and src/telegram_bot/handlers/termination.py using ConversationHandler for multi-step input. Each handler should: parse the message, validate with the Pydantic model, confirm details back to the user, then call the ADP automation (stub the ADP call for now with a placeholder). Write tests for the parsers in tests/test_parsers.py and run them.