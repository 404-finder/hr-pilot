Implement src/adp/auth.py with the login_to_adp function from CLAUDE.md. Include retry logic with exponential backoff (3 attempts), MFA/TOTP handling via pyotp, and proper error handling that raises LoginError on failure. Implement src/adp/navigation.py with navigate_to_new_hire and navigate_to_termination stub functions that include detailed docstrings explaining the expected ADP menu path. These will need to be updated once we inspect ADP's actual DOM.