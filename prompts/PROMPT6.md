Create all selector files: src/adp/selectors/login.py, src/adp/selectors/new_hire.py, src/adp/selectors/termination.py with the placeholder selectors from CLAUDE.md. Mark each constant with a comment "# PLACEHOLDER — update from DevTools". Then implement src/adp/base_form.py with the shared utilities: fill_text_field, select_dropdown, fill_date_field, click_checkbox — all using Playwright async API exactly as specified in CLAUDE.md.